# Core dependencies
litellm>=1.0.0
rich>=13.0.0
pydantic>=2.0.0
openai>=1.0.0

# Web dependencies (optional - install with: pip install -r requirements.txt && pip install -r requirements-web.txt)
# See requirements-web.txt for web server dependencies
